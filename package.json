{
  "name": "Elastic-Beanstalk-Sample-App",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-openid-connect": "^2.15.0",
    "mongodb": "^5.2.0",
    "mongoose": "^7.0.3",
    "path": "^0.12.7"
  },
  "scripts": {
    "start": "cd client && npm install && npm run build && cd .. && node app.js"
  },
  "devDependencies": {
    "typescript": "^5.0.4"
  }
}
