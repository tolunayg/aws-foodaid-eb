{
  "name": "Elastic-Beanstalk-Sample-App",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-oauth2-jwt-bearer": "^1.4.1",
    "express-openid-connect": "^2.15.0",
    "jsonwebtoken": "^9.0.0",
    "mongodb": "^5.2.0",
    "mongoose": "^7.0.3",
    "nodemon": "^2.0.22",
    "path": "^0.12.7"
  },
  "scripts": {
    "build": "GENERATE_SOURCEMAP=false react-scripts build",
    "start": "node app.js",
    "postinstall": "npm cache clean --force && cd client && npm install --no-optional --no-audit && npm run build && cd .."
  },
  "devDependencies": {
    "typescript": "^5.0.4"
  },
  "engines": {
    "node": "16.20.0"
  }
}