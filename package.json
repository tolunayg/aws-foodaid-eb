{
  "name": "Elastic-Beanstalk-Sample-App",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "express": "^4.18.2",
    "mongodb": "^5.2.0",
    "mongoose": "^7.0.3",
    "path": "^0.12.7"
  },
  "scripts": {
    "start": "npm run install-client && npm run build-client && node app.js",
    "install-client": "cd client && npm install",
    "build-client": "cd client && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.0.4"
  }
}
