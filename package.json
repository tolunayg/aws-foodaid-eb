{
  "name": "Elastic-Beanstalk-Sample-App",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "express": "^4.18.2",
    "mongodb": "^5.2.0",
    "mongoose": "^7.0.3",
    "path": "^0.12.7"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node app.js"
  }
}
